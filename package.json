{
  "name": "frontend-project-12-vite",
  "version": "1.0.0",
  "description": "### Hexlet tests and linter status:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky",
    "lint": "npm run lint --workspace=client"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.8.0",
    "husky": "^9.1.4"
  }
}
